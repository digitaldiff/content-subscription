{% import '_includes/forms' as forms %}

    {{ forms.selectField({
        label: 'Group'|t('content-subscriptions'),
        name: 'groupId',
        value: subscription.groupId,
        options: groups,
        required: true,
        errors: subscription.getErrors('groupId'),
        first: true
    }) }}

    {{ forms.textField({
        label: 'Firstname'|t('content-subscriptions'),
        name: 'firstName',
        required: true,
        errors: subscription.getErrors('firstName'),
        value: subscription.firstName
    }) }}

    {{ forms.textField({
        label: 'Lastname'|t('content-subscriptions'),
        name: 'lastName',
        required: true,
        errors: subscription.getErrors('lastName'),

        value: subscription.lastName
    }) }}

    {{ forms.textField({
        label: 'Email'|t('content-subscriptions'),
        name: 'email',
        required: true,
        errors: subscription.getErrors('email'),

        value: subscription.email
    }) }}