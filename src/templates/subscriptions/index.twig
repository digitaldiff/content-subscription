{% extends '_layouts/elementindex.twig' %}
{% set title = 'Products' %}
{% set elementType = 'publishing\\mailsubscriptions\\elements\\Subscription' %}


{#
{% extends '_layouts/cp' %}

{% import '_includes/forms' as forms %}

{% set title = 'Subscriptions'|t('mail-subscriptions') %}
{% set groups = craft.mailSubscriptions.getMailGroups %}

{% block headertitle %}
    <h1>{{ title }}</h1>
{% endblock %}

{% block actionButton %}
    <a href="{{ cpUrl('mail-subscriptions/subscriptions/new') }}" class="btn submit add icon">{{ 'New Group'|t('mail-subscriptions') }}</a>
{% endblock %}

{% block content %}

    {% set subscriptions = craft.mailSubscriptions.getSubscriptions() %}

    {% if subscriptions|length %}
        <div class="tableview tablepane">
            <table class="data fullwidth">
                <thead>
                    <tr>
                        <th scope="col">{{ 'Group ID' |t('mail-subscriptions') }}</th>
                        <th scope="col">{{ 'Firstname' |t('mail-subscriptions') }}</th>
                        <th scope="col">{{ 'Lastname' |t('mail-subscriptions') }}</th>
                        <th scope="col">{{ 'Email' |t('mail-subscriptions') }}</th>
                        <th class="thin"></th>
                        <th class="thin"></th>
                    </tr>
                </thead>
                <tbody>
                    {% for subscription in subscriptions %}
                        <tr class="g-{{ subscription.groupId }}">
                            <td>{{ subscription.groupId }}</td>
                            <td>{{ subscription.firstName }}</td>
                            <td>{{ subscription.lastName }}</td>
                            <td>{{ subscription.email }}</td>
                            <td><a href="{{ cpUrl('mail-subscriptions/subscriptions/edit/' ~ subscription.id) }}" class="edit icon" title="{{ 'Edit'|t('app') }}" role="button"></a></td>
                            <td><a href="{{ cpUrl('mail-subscriptions/subscriptions/delete/' ~ subscription.id) }}" class="delete icon" title="{{ 'Delete'|t('app') }}" role="button"></a></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}

{% endblock %}#}
