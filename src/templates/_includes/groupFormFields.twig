{% import '_includes/forms' as forms %}

{#{% set formActions = [
    {
        label: 'Save and continue editing'|t('mail-subscriptions'),
        redirect: 'mail-subscriptions/groups/edit/{group.id}',
        retainScroll: true,
        shortcut: true,
    },
    {
        label: 'Save and add another'|t('mail-subscriptions'),
        redirect: 'mail-subscriptions/groups/new',
        shortcut: true,
        shift: true,
    },
    {
        label: 'Delete'|t('mail-subscriptions'),
        confirm: 'Are you sure you want to delete this?'|t('plugin-handle'),
        redirect: 'mail-subscriptions/groups',
        destructive: true,
    },
] %}#}

    {{ forms.selectField({
        label: 'Section'|t('mail-subscriptions'),
        name: 'section',
        value: group.sectionId,
        options: sections
    }) }}

    {{ forms.textField({
        label: 'Group name'|t('mail-subscriptions'),
        name: 'groupName',
        value: group.groupName
    }) }}

    {{ forms.textField({
        label: 'Mail subject'|t('mail-subscriptions'),
        name: 'emailSubject',
        value: group.emailSubject
    }) }}

    {{ forms.textAreaField({
        label: 'Mail Message'|t('mail-subscriptions'),
        name: 'emailMessage',
        value: group.emailBody,
        rows: 8
    }) }}