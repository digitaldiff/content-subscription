{% import '_includes/forms' as forms %}

    {{ forms.lightswitchField({
        label: "Enabled"|t('mail-subscriptions'),
        name: 'enabled',
        on: (element) ? element.enabled : ''
    }) }}