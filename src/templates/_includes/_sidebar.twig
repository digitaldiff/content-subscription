{% import '_includes/forms' as forms %}

    {{ forms.lightswitchField({
        label: "Enabled"|t('content-subscriptions'),
        name: 'enabled',
        on: (element) ? element.enabled : ''
    }) }}